# This file is maintained for legacy compatibility and Docker builds.
# The source of truth for dependencies is pyproject.toml
#
# To regenerate this file from pyproject.toml:
#   pip install -e .
#   pip freeze > requirements.txt
#
# For development, use: pip install -e ".[dev]"

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
